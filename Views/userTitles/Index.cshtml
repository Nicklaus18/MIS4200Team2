@model IEnumerable<MIS4200Team2.Models.userTitle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.titleUser)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.titleUser)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.usertitleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.usertitleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.usertitleID })
        </td>
    </tr>
}

</table>
