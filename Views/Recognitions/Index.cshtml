@model IEnumerable<MIS4200Team2.Models.Recognition>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-hover table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Users.fullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CoreValues.CoreValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descriptionOfValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Users.fullName) 
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CoreValues.CoreValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descriptionOfValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RecognitionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RecognitionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RecognitionID })
        </td>
    </tr>
}

</table>
